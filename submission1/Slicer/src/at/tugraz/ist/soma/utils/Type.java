package at.tugraz.ist.soma.utils;

/**
 * this enum represents the type of a {@link at.tugraz.ist.soma.statements.Statement}.
 * @author Stephan Frühwirt
 */
public enum Type {
	ASSIGNMENT,
	VARIABLE_DECLARATION,
	LOOP_FOR,
	LOOP_WHILE,
	IF_STMT,
	ELSE_STMT,
	STATEMENT;
}
